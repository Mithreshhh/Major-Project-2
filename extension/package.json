{
  "name": "@odpa/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 browser extension (Chrome + Firefox): captures the page, runs on-device perception + redaction, asks the server for an action, executes it.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  }
}
