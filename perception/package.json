{
  "name": "@odpa/perception",
  "version": "0.1.0",
  "private": true,
  "description": "On-device perception (ONNX Runtime Web + lightweight ViT) and privacy redaction that run inside the browser extension.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "onnxruntime-web": "^1.29.0"
  }
}
